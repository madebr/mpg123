Dammit, this is a dummy file! Checking in the real changelog into the version system is too weird since it just consists of the log of the version system.

Thomas: Please do

svn revert ChangeLog

after a release!

(or modify the make dist target to do that...)
